## ✅ 第二部分：升级版任务清单（中文）

| 阶段 | 任务 | 交付物 | 成功标准 |
|------|------|--------|----------|
| **1. 建立基线** | 实现 HF 原生推理 + 显存/吞吐监控 | `scripts/baseline.py`<br>`results/baseline.csv` | 能复现 `model.generate()` 行为；测量误差 <5% |
| **2. 设计接口** | 定义 `KVCacheManager` 抽象类<br>修改 `LlamaAttention` 支持注入 | `kv_manager/base.py`<br>patched `modeling_llama.py` | 新策略只需继承接口，无需修改模型核心代码 |
| **3. 实现策略** | 完成 Prefix / CPU Offload / Sliding Window | `kv_manager/prefix_cache.py` 等 | 每个策略核心逻辑 ≤100 行 |
| **4. 统一评测** | 编写 `benchmark.py`<br>支持多策略、多 prompt、多长度 | `scripts/benchmark.py` | 输出结构化 CSV：<br>`strategy, context_len, gpu_mem_gb, tokens_per_sec` |
| **5. 分析洞察** | 生成对比图表<br>回答 4 个研究问题 | `results/comparison.md`<br>`results/plots/` | 能解释“何时用哪种策略最优” |
| **6. 文档包装** | 编写专业 README<br>整理项目结构 | `README.md` | 外人能在 10 分钟内跑通 baseline |

---

## 💼 简历项目描述（中文，可直接用）

> **KVCraft：KV Cache 优化策略研究与实现**  
> 在原生 Hugging Face Transformers 框架上实现并评测 4 种 KV Cache 策略（Prefix Caching、CPU Offload、Sliding Window 等），量化其在 Llama-2-7B 上的显存（↓40%）、吞吐（±20%）与上下文长度（↑16×）的权衡；设计可插拔缓存接口，支持公平对比；所有实验可复现，代码开源。